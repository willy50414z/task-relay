# Implementation Draft Packet

Task id: `2.1`
Mode: `implementation-draft`
Objective: Draft a helper that scans project and user guidance candidates for task-relay managed blocks and returns unambiguous existing configuration metadata for `trly install` prefill.

Context files:
- `openspec/changes/improve-install-wizard-ux/design.md`
- `openspec/changes/improve-install-wizard-ux/specs/keyboard-install-wizard/spec.md`
- `openspec/changes/improve-install-wizard-ux/tasks.md`
- `task_relay/delegation.py`
- `task_relay/wizard.py`
- `task_relay/cli/__init__.py`

Allowed direct reads:
- Read task `2.1` and related tasks `2.2` through `2.4` in `openspec/changes/improve-install-wizard-ux/tasks.md`.
- Read `D3: Resolve existing state before prompting` in `openspec/changes/improve-install-wizard-ux/design.md`.
- Read `Requirement: Existing configuration prefill` in `specs/keyboard-install-wizard/spec.md`.
- Read current `parse_existing_block`, `resolve_install_paths`, and guidance file constants in `task_relay/delegation.py`.

Non-goals:
- Do not update OpenSpec scope.
- Do not mark `tasks.md` checkboxes.
- Do not perform destructive operations.
- Do not make architecture, security, credential, or migration decisions.
- Do not modify files.

Expected output:
- A small implementation draft or file-by-file edit plan for Codex review.
- Include proposed helper names, return shape, and edge-case handling for zero, one, or multiple existing blocks.
- Call out tests that should be written first.

Verification command:
- `pytest tests/test_wizard.py tests/test_cli.py`

Timeout guidance:
- Default to 600 seconds.
